# Python зависимости для Render
# Устанавливаются командой: pip install -r requirements.txt

fastapi==0.111.0          # веб-фреймворк (наш сервер)
uvicorn==0.29.0           # ASGI сервер (запускает FastAPI)
mysql-connector-python==8.4.0  # подключение к MySQL на Бегете
google-generativeai==0.7.2     # Gemini API (замена OpenAI, бесплатно)
python-multipart==0.0.9        # загрузка файлов через форму
reportlab==4.2.0               # PDF fallback (если нет LibreOffice)
requests==2.32.0               # HTTP запросы (для скачивания фото)
